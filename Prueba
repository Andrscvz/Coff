clase coff{
	atributos:
		entero a;
		texto b = "a";
		decimal cc = 3.0;
	metodos:
		funcion entero manuel(entero c, decimal &d){
			entero m;
			coff k;
			retorna 1;
		}
		
		funcion entero andres(){
			retorna 1;
		}
}

funcion entero dos(entero b){
	asigna b = b * 2;
	si (b < 3) {
		asigna b = dos(dos(1));
	}
	
	retorna b;
} 

principal vacio inicio(){

	entero j;
	decimal y;

	coff k,h;
	ejec k.manuel(dos(dos(1)),y);


	


}